var headerMap = {
    agenda: "Agenda",
    review: "Part 1: Intro and Review"
};

// Reveal.addEventListener( 'slidechanged', function( event ) {
//     if (event.currentSlide.dataset.updateHeader) {
//         var header = document.querySelector('.slide-header');
//         header.innerHTML = headerMap[event.currentSlide.dataset.updateHeader] || "";
//     }
// } );
